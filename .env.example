# Copy this file to ".env" and adjust values before running the MCP servers locally.

# Sets development-friendly defaults for local debugging.
NODE_ENV=development

# Required: MoCo API credentials
# Replace with your personal API key from MoCo.
MOCO_API_KEY=your-moco-api-key
# Replace with your MoCo subdomain (e.g. "acme" for https://acme.mocoapp.com).
MOCO_SUBDOMAIN=your-subdomain
# Cache duration for MoCo list responses (seconds). Defaults to 300 (5 minutes). Set to 0 to disable caching.
#MOCO_API_CACHE_TIME=300

# Optional: HTTP server configuration
# Port exposed by the HTTP transport (defaults to 8080 when omitted).
#MCP_HTTP_PORT=8080
# Host binding for the HTTP server. 0.0.0.0 allows external access; 127.0.0.1 restricts to local machine.
#MCP_HTTP_HOST=0.0.0.0
# Base path where the HTTP endpoint is mounted. Useful when reverse-proxying.
#MCP_HTTP_PATH=/sse
# Control whether the server maintains session state (true) or treats requests statelessly (false).
# Defaults to stateless (false) for compatibility with load-balanced HTTP clients.
#MCP_HTTP_SESSION_STATEFUL=false
# Comma-separated list of hosts allowed to reach the server (enables DNS rebinding protection when set).
#MCP_HTTP_ALLOWED_HOSTS=
# Comma-separated list of origins allowed for CORS requests.
#MCP_HTTP_ALLOWED_ORIGINS=

# Optional: Logging configuration
# Log level for the MCP servers. Supported values: debug, info, warn, error. Defaults to info when omitted.
LOG_LEVEL=info

# Optional: ngrok tunneling (required when exposing the HTTP transport externally)
# Toggle automatic ngrok tunnel in the HTTP server (defaults to disabled)
#NGROK_ENABLED=false
# Authentication token from your ngrok dashboard. Needed for npm run tunnel:http
#NGROK_AUTHTOKEN=your-ngrok-authtoken
# Preferred ngrok region (e.g. eu, us). Leave unset to let ngrok pick automatically.
#NGROK_REGION=eu
# Reserved domain to use for the tunnel (requires paid ngrok plan)
#NGROK_DOMAIN=your-domain.ngrok.app
